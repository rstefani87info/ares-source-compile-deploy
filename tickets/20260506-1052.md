# Checklist Migliorie aReS @ares/scd

## Alta Priorita
- [x] Standardizzare docs/it e docs/en con struttura comune (scopo/install/quickstart/api/config/test)
- [x] Rafforzare esempi d’uso reali (quickstart) e casi d’uso
- [x] Aggiungere/rafforzare smoke test

> Nota: standardizzazione docs completata in docs/it/scd.md e docs/en/scd.md.
> Nota: il modulo ha assorbito il metamodel utile di `@ares/programming` in `programming.js`, espone API reali dal root e include smoke test in `test/smoke.test.js`.

## Contratto Modulo (API Pubbliche)
- [x] Documentare entrypoint stabili vs opzionali vs transizionali
- [x] Aggiornare esempi per aderire a import ESM con estensione quando possibile

## Packaging
- [x] Verificare `exports` (se usato) e compatibilità import ESM/CJS
- [ ] Verificare `types` e file pubblicati

> Nota: aggiunti `"type": "module"`, `bin` `ares-scd` e mappa `exports` per root, manager, git, git-flow, cli e programming.

## Dipendenze e Peer Dependencies
- [x] Verificare dipendenze runtime vs devDependencies
- [ ] Dichiarare peerDependencies dove necessario

## Robustezza Runtime
- [x] Gestire errori e fallback di configurazione in modo esplicito
- [x] Evitare side effects non opt-in (logging, IO, network) se possibile

> Nota: rimossi `commander`/`chalk`/`figlet` non dichiarati dalla CLI, consolidati Git/GitFlow e corretti import ESM senza estensione.

## Qualita' (Test e Lint)
- [x] Aggiungere/eseguire test (`yarn workspace ... test`)
- [ ] Aggiungere lint/format se previsto dal workspace

> Nota: verificato con `node --test .\scd\test\*.test.js` (4 test passati).

## Documentazione e Adozione
- [x] Completare sezione configurazione con chiavi realmente consumate
- [x] Aggiungere riferimenti incrociati verso moduli dipendenti (core/web/files, ecc.)
